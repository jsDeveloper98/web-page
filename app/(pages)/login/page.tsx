import { LoginForm } from "@/components/loginForm";

const LoginPage = () => {
  return (
    <div className="LoginPage flex items-center justify-center h-full">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
