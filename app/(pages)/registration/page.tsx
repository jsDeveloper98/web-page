import { RegistrationForm } from "@/components/registrationForm";

const RegistrationPage = () => {
  return (
    <div className="RegistrationPage flex items-center justify-center h-full">
      <RegistrationForm />
    </div>
  );
};

export default RegistrationPage;
